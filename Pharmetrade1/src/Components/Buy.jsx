import React from 'react'

const Buy = ({ topMargin }) => {
  return (
    <div
    className="w-full h-full flex justify-center items-center"
    style={{ marginTop: `${topMargin + 20}px` }}
  ><div className='w-[95%] h-full'>jhbhg

  </div>
    </div>
  )
}

export default Buy
